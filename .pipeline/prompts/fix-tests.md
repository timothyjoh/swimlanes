Tests are failing after the build step. Here is the test output:

{{TEST_OUTPUT}}

Fix all failing tests. Read CLAUDE.md for the test command. Run tests to verify everything passes before committing.
